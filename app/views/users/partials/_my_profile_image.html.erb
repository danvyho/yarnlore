<% if post.image.attached? %>
  <div class="flex-shrink-0">
    <%= cl_image_tag post.image.key,
          class: "img-fluid",
          style: "object-fit: cover; height: 150px; min-width: 120px; border-right: 3px solid #f1f1f1; border-top-left-radius: 1rem;" %>
  </div>
<% end %>
