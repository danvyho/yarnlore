<div class="row">
  <% @posts.each do |post| %>
    <div class="col-sm-3 mb-4">
      <a href="<%= post_path(post) %>" style="text-decoration: none; color: inherit;">
        <%= render 'posts/partials/post_card', post: post, current_user: current_user %>
      </a>
    </div>
  <% end %>
</div>
</div>
